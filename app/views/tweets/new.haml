%h1 New tweet
= form_for @tweet do |f|
  - if @tweet.errors.any?
    .alert-box.warning
      - @tweet.errors.full_messages.each do |error|
        = error
        %br
  = f.text_area :message
  = f.submit