this line is just debug line, to show alerts are picked up
- if notice
  .alert-box
    = notice
    %a.close{href: ''} &times;
- if alert
  .alert-box.warning
    = alert
    %a.close{href: ''} &times;